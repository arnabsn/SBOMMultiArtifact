pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'build'
            }
        }
        stage('Change') {
            steps {
                echo 'change'
            }
        }
    }
}
